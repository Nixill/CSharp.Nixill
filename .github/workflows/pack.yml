# thanks to https://github.com/SierraSoftworks/sierralib.api.views/blob/master/.github/workflows/package.yml for helping me figure this out
# GitHub's own documentation was a bit lacking.
name: Publish to GitHub Packages

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the project
        uses: actions/checkout@master
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '3.1.100'
      - name: Setup nuget
        uses: nuget/setup-nuget@v1
        with:
          nuget-version: 'latest'
      - name: Test the package
        run: dotnet test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_VERSION: ${{ github.event.release.tag_name }}
      - name: Pack the thing up
        run: |
          cd CS-NixLib/
          dotnet pack -o out
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_VERSION: ${{ github.event.release.tag_name }}
      - name: Push to github release
        uses: actions/upload-release-asset@v1.0.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./CS-NixLib/out/CS-NixLib.${{ github.event.release.tag_name }}.nupkg
          asset_name: CS-NixLib.${{ github.event.release.tag_name }}.nupkg
          asset_content_type: application/zip
      - name: Push to nuget.org
        run: |
          cd CS-NixLib
          for f in ./out/*.nupkg
          do
            dotnet nuget push AppLogger.1.0.0.nupkg --api-key ${{ secrets.NUGET_TOKEN }} --source https://api.nuget.org/v3/index.json
          done